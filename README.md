# ꙰ Yatharth — Starter Collection

Bilingual manifesto · Meditation audio · Vision assets

## Quick start
1. Edit `data/items.json` to add your audio metadata (see format).
2. Upload previews on Drive (or `assets/previews/`), ensure public sharing for previews.
3. Push to `main` branch — site is live via GitHub Pages.

## Proceeds & support
Proceeds support Saneha Saini.

## Phase B (production)
For automated delivery, use Firebase Functions + Google Cloud Storage (signed URLs). See `functions/` for sample.
